on: [push]

jobs:
  hamownia_mcb_esp32_job:
    runs-on: ubuntu-latest
    name: Hamownia docker test
    steps:
      # To use this repository's private action,
      # you must check out the repository
      - name: Checkout
        uses: actions/checkout@v3
      - name: 